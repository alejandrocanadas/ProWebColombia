"use client"; // This is a client component that shows an error page it has to be noted with "use client" directive
const Error = () => {
    return (
        <div>
            <h1>Error</h1>
        </div>
    )
} 

export default Error;